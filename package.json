{
  "name": "wallet-balance-notifier",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.2",
    "dotenv": "^8.2.0",
    "googleapis": "^42.0.0",
    "gsheet-object": "0.1.3",
    "web3-utils": "^1.2.6"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^18.8.3",
    "jest": "26.6.1",
    "rimraf": "^3.0.2",
    "ts-jest": "26.4.2",
    "ts-node": "^8.8.2",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "build:cp-package-json": "grep -v \"\\\"build\" ./package.json | tee ./build/package.json > /dev/null",
    "build": "rimraf build && mkdir build && yarn build:cp-package-json && tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "start": "ts-node src/run.ts",
    "deploy": "yarn build && yarn deploy:function",
    "deploy:function": "gcloud functions deploy sync-wallet-balance --entry-point main --source dist --runtime=nodejs20"
  }
}
